<div class="row">
    {{#each marca}}
    <div class="col-md-4">
        <div class="card border-dark mb-3">
            <div class="card-header">
                <strong class="mr-2"> Código: </strong> {{ID_MARCA}}
            </div>
            <div class="card-body">
                <h5 class="pb-2"> {{NOMBRE_MARCA}} </h5>
                <div class="text-right">
                    <a href="/productos/marca/delete/{{ID_MARCA}}" class="btn btn-danger btn-sm"> Eliminar </a>
                    <a href="/productos/marca/edit/{{ID_MARCA}}" class="btn btn-secondary btn-sm"> Editar </a>
                </div>
            </div>

        </div>
    </div>

    {{else}}
    <div class="col-md-8 mx-auto">
        <div class="card card-body text-center">
            <p class="mt-3"> No existen marcas de producto </p>
        </div>
    </div>
    {{/each}}
</div>